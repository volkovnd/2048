{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@vueuse/core": "^11.0.3",
    "normalize.css": "^8.0.1",
    "vue": "latest"
  },
  "devDependencies": {
    "@vueuse/nuxt": "^11.0.3",
    "nuxt": "^3.13.0"
  }
}
